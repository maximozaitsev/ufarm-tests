[pytest]
markers =
    api: API tests
    ui: UI tests
    trx: Transaction (on-chain) tests
    smoke: Smoke suite — critical happy-path checks
    regression: Regression suite — standard feature checks
    extended: Extended suite — edge cases and deep validation

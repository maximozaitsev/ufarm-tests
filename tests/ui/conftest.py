# Скриншоты при падении обрабатываются в корневом conftest.py через хук
# pytest_runtest_makereport — это берёт скриншот ДО teardown фикстур,
# пока страница гарантированно живая и показывает состояние в момент сбоя.
